// This page redirect is now handled by middleware
// This component should rarely render as middleware handles the redirect
export default function BusinessesPage() {
  // This is a fallback - middleware should handle the redirect
  return null;
}